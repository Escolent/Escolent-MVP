// Shared between /api/auth/google/login and /api/auth/google/callback —
// see the equivalent comment on the LTI cookie module for why this lives
// outside the route files.
export const GOOGLE_PENDING_LOGIN_COOKIE = "escolent_google_pending";
